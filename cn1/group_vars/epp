# sudo
default_sudo_specs:
    - type: permission
      user: '%syseng'
      host: ALL
      runas: ALL
      command: ALL

    - type: permission
      user: '%neteng'
      host: ALL
      runas: ALL
      command: ALL

    - type: permission
      user: '%regops'
      host: ALL
      runas: ALL
      command: ALL

    - type: permission
      user: '%noc'
      host: ALL
      runas: ALL
      command: '/bin/su - nagios, /bin/su - nocmon, /etc/init.d/nrpe restart, /etc/init.d/pandora_agent_daemon restart, /usr/bin/pandora_agent, /bin/su - graylog, /sbin/service collector-sidecar restart, /sbin/service graylog-sidecar restart, /bin/systemctl restart graylog-sidecar'

puppet2016_enabled: true

# host members
default_members:
    - bkanagar
    - cchao
    - fsayed
    - gopal
    - gsp
    - kzhao
    - mpacyna
    - parinya
    - rbhardwa
    - mweissha
    - slimba
    - ksyed
    - jyeum
    - atalan
    - fmosavat
    - sgrover
    - rprasad
    - kborkako

    
